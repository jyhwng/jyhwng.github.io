{
  "name": "calmels-and-snakes",
  "description": "Jyhwng's personal tech blog",
  "version": "1.0.0",
  "author": "Jiyoung Hwang",
  "dependencies": {
    "gatsby": "^1.9.207",
    "gatsby-link": "^1.6.37",
    "gatsby-plugin-favicon": "^2.1.1",
    "gatsby-plugin-react-helmet": "^2.0.5",
    "gatsby-remark-emoji": "^0.0.1",
    "gatsby-remark-prismjs": "^1.2.17",
    "gatsby-source-filesystem": "^1.5.23",
    "gatsby-transformer-remark": "^1.7.33",
    "react-helmet": "^5.2.0",
    "styled-components": "^3.1.6"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "rm -rf .cache && gatsby build",
    "develop": "rm -rf .cache && gatsby develop",
    "deploy": "rm -rf .cache && gatsby build && gh-pages -d public -b master",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"src/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.10.0",
    "gatsby-plugin-styled-components": "^3.0.7",
    "gh-pages": "^1.1.0",
    "prettier": "^1.10.2"
  }
}
